# git reset
# git commit -m "first commit"
#  git branch -M main
# git remote add origin git@github.com:naiduharinadh/cognito-js.git
# git push -u origin main

> update the url variables and secrets in the code
> need to update the endpoint "/login", "/home" as it is the one which is acting as a home page after the authentication.
